# jellyfin-music-uploader
A Jellyfin plugin that adds an **Add Music** button to the web UI. Drag one or more `Artist/Album/song` folders into the modal, optionally pick a cover image per album, and the plugin writes the files into your music library folder and triggers a library scan. 
